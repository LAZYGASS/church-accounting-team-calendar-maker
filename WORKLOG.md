# 작업 기록

## 2026-09-08 — 날짜·다운로드·모바일 개선

- 요구사항: TASKS.md의 R8-01~03. 기존 정적 웹 구조를 유지한다.
- 변경: 웹 일정 입력 검증, 표시 연도 기반 파일명, 다운로드 중복 방지·오류 안내·버튼 복구, 폰트 준비 후 고해상도 캡처. 모바일 카드 너비 제한 및 작은 화면의 이벤트 표시 조정.
- 검증: `node --test tests/calendar.test.cjs` 4건 통과. 2000~2100년 1,212개월 날짜 규칙, 다운로드 중복, 실패 후 재시도, CDN 미로드 안내 검증. 실패·건너뜀 없음.
- 진행 중: Python 요일 정렬 및 운영위원회 수정의 실행 검증, 브라우저 화면·실제 이미지 저장 검증, 관련 문서 정합성 확인.

### Python 계산 검증 및 모바일 재검증
- Python 실제 계산 메서드를 AST로 추출하여 표준 라이브러리만으로 실행. 2000~2100년 전체 월을 월요일/일요일 시작 설정 각각 검증하여 통과(2,424개 조합).
- Python 환경에 `python-pptx`가 없어 PPT 생성 전체 실행은 아직 검증하지 못했다.
- 375px 브라우저에서 미디어 쿼리의 그리드 자동 최소 너비로 가로 넘침을 발견하여 `minmax(0, 1fr)`로 수정. 후속 화면 확인 진행.
- 수정 후 375px 화면 스크린샷에서 7개 요일과 이벤트가 화면 안에 표시되는 것을 확인했다.
- README에 현재 규칙과 검증 명령을 명시하고 과거 PRD/요약 문서는 역사 기록임을 표시했다. 웹 안내와 기존 날짜 확인 스크립트의 잘못된 요일 문구도 정정했다.
- 이미지 저장 요청은 브라우저에서 실행되지만 자동화 도구가 다운로드 취소를 보고하여 저장 파일 검증은 계속 진행 중이다.

### 2026-09-10 — PPT 생성 검증
- `.verification/venv`에 기존 의존성 `python-pptx`를 격리 설치했다. 설치 성공, pip 업데이트 알림만 있으며 업데이트하지 않았다.
- `tests/test_ppt_calendar.py` 통과: 실제 12개 슬라이드 생성 후 모든 현재 월 날짜가 Sun~Sat 머리글과 일치하고 운영위원회 날짜가 정확한지 확인했다. 기존 PPT 산출물은 변경하지 않았다.

### 푸시 전 최종 검토
- PNG 저장을 Blob URL로 처리하고 사용 후 URL을 해제하도록 변경. 변경 후 Node 테스트 4건 재통과(실패·건너뜀 없음).
- 실제 브라우저에서 캡처 및 저장 요청 안내까지 실행되었으나 `agent-browser download`는 반복해서 `Download was canceled`를 반환했다. 사용자 파일 저장 완료 및 저장 이미지 시각 검증은 UNVERIFIED이며, 완료로 간주하지 않는다.
- R8-01·03 검증 완료, R8-02 구현·단위 검증 완료 및 실제 파일 저장 검증 미완료를 TASKS.md에 명시했다.
- 사용자 요청에 따라 현재 변경분을 커밋·푸시한다. 검증 가상환경은 `.verification/`에 격리하고 Git에서 제외했다. git diff 검토에서 요청과 무관한 변경은 확인되지 않았다.

### 원격 최신 변경 통합
- 첫 푸시가 non-fast-forward로 거절되어 원격 main을 fetch했다. 원격에는 2026년 6월 이후 토요일 집행, 2026년 3·4월 운영위원회 예외, 현재 연도 자동 선택 및 배포 문서가 이미 존재했다. 최초 로컬 기준의 화요일 고정 설명은 최신 규칙으로 정정했다.
- 원격 기능을 보존하고 다운로드·모바일 개선을 병합했다. Python에도 웹과 같은 3·4월 예외를 반영하고 README를 최신 규칙으로 수정했다.
- 병합 후 Node 4건, Python 날짜 1건(2,424개 조합), 실제 PPT 표 생성 1건(12개월) 재통과. 실패·건너뜀 없음.
- 병합 후 375px 실제 화면에서 6월 배너·토요일 집행·전주 결재·운영위원회·다운로드 버튼의 표시를 확인했다. `git diff --check` 통과. LF/CRLF 변환 안내는 저장소 설정에 따른 알림이며 기능 오류는 아니다.
